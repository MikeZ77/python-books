# The momento pattern provides the ability to restore an object to its ...
# previous state.

# Recall that with the command pattern, a command perform undo by popping ...
# whatever data it is using off the stack.

# The memento pattern allows you to rollback to ANY of the previous states.

